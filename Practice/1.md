---
layout: default
title: 연습 1
parent: 연습 문제
nav_order: 1
math: mathjax
---

## **✍️ 연습 문제**

### **1. 리스트 분석 함수**
아래 list1 데이터를 활용하여 다음 6가지 기능을 수행하는 함수를 조건에 맞게 각각 작성하세요.

- `list1= [2, 5, 6, 7, 8, 13, 15, 17, 18, 23, 24, 37, 39, 40, 41, 44, 45, 47, 50]`

1. print_list(data): 리스트의 모든 요소를 순서대로 **출력**하세요.
   - 조건: 'for element in data:' 구문 사용할 것

2. print_idx(data): 리스트의 인덱스와 요소를 함께 **출력**하세요. (예: 인덱스 0: 2)
   - 조건: `len()`과 `range()` 함수를 조합하여 사용할 것

3. get_len(data): 리스트에 담긴 요소의 총 개수를 **반환**하세요.
   - 조건: `len()` 함수를 사용하지 말고 `for` 문으로 직접 셀 것

4. print_3x(data): 리스트 요소 중 3의 배수만 골라 **출력**하세요.
   - 조건: `if` 조건문을 사용할 것

5. num_3x(data): 리스트 내 3의 배수가 총 몇 개인지 **반환**하세요.
   - 조건: `count()` 함수를 사용하지 말 것

6. list_3x(data): 3의 배수만 따로 모은 새로운 리스트를 만들어 **반환**하세요.
   - 조건: `append()` 함수를 사용할 것

```python
list1 = [2, 5, 6, 7, 8, 13, 15, 17, 18, 23, 24, 37, 39, 40, 41, 44, 45, 47, 50]

# 1. 단순 요소 출력
def print_list(data):


# 2. 인덱스를 이용한 출력 (위치 이동)
def print_idx(data):


# 3. 요소 개수 직접 세기
def get_len(data):


# 4. 3의 배수만 출력
def print_3x(data):


# 5. 3의 배수 개수 반환
def num_3x(data):


# 6. 3의 배수 리스트 생성
def list_3x(data):


# --- 실행 파트 ---
print("1. 전체 데이터 확인:")
view_list(list1)

print("\n2. 인덱스별 데이터 확인:")
view_idx(list1)

print(f"\n3. 리스트 길이: {get_len(list1)}")

print("\n4. 3의 배수들:", end=' ')
view_3x(list1)

print(f"5. 3의 배수 개수: {get_3x_cnt(list1)}")
print(f"6. 추출된 리스트: {get_3x_list(list1)}")
```

---

### **2. STM 표면 스캔 시뮬레이션 (Nested Loop 응용)**

$\text{Cu}$ (구리) 표면 위에 흩어져 있는 5개의 $\text{H}$ (수소) 원자를 찾아내는 STM 스캔 과정을 시뮬레이션해 봅시다.

![](https://bgjang-khu.github.io/MSE201/assets/images/stm.png)

**함수 작성 및 수행 조건**

1. 환경 구축: 10 $\times$ 10 Atom Array 생성  

   실험 대상인 구리($\text{Cu}$) 기판을 파이썬 배열로 정의합니다. 이 배열은 가로 10개, 세로 10개의 원자가 격자 형태로 배치된 10 $\times$ 10 Atom Array입니다.

   - 배열 생성: `np.full((10, 10), 29)`를 사용하여 모든 칸을 $\text{Cu}$의 원자 번호인 29로 채운 2차원 배열을 만듭니다.

   - 좌표계 이해 ($i, j$):
     - $i$: 행(Row) 인덱스로, 위에서 아래로 이동하는 세로축 좌표를 의미합니다.
     - $j$: 열(Column) 인덱스로, 왼쪽에서 오른쪽으로 이동하는 가로축 좌표를 의미합니다.
     - $(i, j)$는 STM 탐침(Probe)이 가리키는 특정 원자의 격자 위치가 됩니다.

2. 불순물 삽입: 배열 내 임의의 위치 5곳에 $\text{H}$의 원자 번호인 1을 배치하세요.

3. `run_scan(surface)` 함수 작성:

   - **중첩 루프(for문 2개)**를 사용하여 배열의 모든 요소를 $(0, 0)$부터 $(9, 9)$까지 차례대로 검사합니다.

   - 값이 1인 위치를 발견하면 `H 원자 발견! 위치: (i, j)`라고 출력합니다.

   - 발견된 수소($\text{H}$) 원자의 위치 $(i, j)$를 튜플(tuple) 형태로 묶으세요.
 
   - 이 튜플들을 found_h라는 하나의 리스트에 차곡차곡 담아(append) 최종적으로 반환하세요.  
       - 예시:  `[(1, 3), (4, 2), ...]`


4. 실행 예시

> {: .result .fs-3 }
> > --- STM 스캔을 시작합니다 ---   
> > 📍 [발견] 좌표 (2, 7)에서 H 원자가 탐지되었습니다.  
> > 📍 [발견] 좌표 (4, 2)에서 H 원자가 탐지되었습니다.  
> > 📍 [발견] 좌표 (6, 9)에서 H 원자가 탐지되었습니다.  
> > 📍 [발견] 좌표 (7, 8)에서 H 원자가 탐지되었습니다.  
> > 📍 [발견] 좌표 (9, 1)에서 H 원자가 탐지되었습니다.  
> > --- 스캔 완료 ---

<\br>

```python
import numpy as np
import random

# 1. 표면 초기화 및 H 원자 임의 배치 함수
def init_surface():
    # 10x10 배열을 Cu(29)로 채움
    surface = np.full((10, 10), 29)
    
    # 5개의 H(1)를 랜덤한 위치에 배치
    h_positions = []
    while len(h_positions) < 5:
        r, c = random.randint(0, 9), random.randint(0, 9)
        if (r, c) not in h_positions:
            surface[r, c] = 1
            h_positions.append((r, c))
            
    return surface

# 2. 표면 스캔 함수 (Nested Loop 핵심)
def run_scan(surface):
    found_h = []  # H 발견 시 저장할 list 생성
    print("--- STM 스캔을 시작합니다 ---")
    
    # 10 by 10 array 행열 탐색하여 원자번호가 1이면 수소로 판단
    # 수소의 위치를 튜플 형태 (i, j) 로 found_h list에 저장

                
    print("--- 스캔 완료 ---")
    return found_h

# --- 메인 실행 파트 ---
# 표면 생성
exp_surface = init_surface()

# 스캔 실행 및 결과 저장
discovered_positions = run_scan(exp_surface)

# 결과 보고
print(f"\n최종 리포트: 총 {len(discovered_positions)}개의 H 원자를 찾아냈습니다.")
print(f"발견된 좌표 리스트: {discovered_positions}")\
```

---

### **3. 로또 번호 생성기 (while 문과 리스트 응용)**

제시된 조건에 따라 1부터 45 사이의 중복되지 않는 정수 6개를 생성하는 lotto() 함수를 완성해 봅시다.

**함수 작성 및 수행 조건**

1. 임의의 숫자 생성
   - 난수 생성을 위해 `random` 모듈 불러오기: `import random`
   - 1 부터 45 사이의 랜덤 숫자 생성: `num=random.randint(1,45)`

2. `lotto()` 함수 정의 
   - Step 1: 생성된 숫자를 보관할 빈 리스트 (`lotto_list`)를 만듭니다.
   - Step 2: 리스트에 담긴 숫자가 6개 미만인 동안 계속 반복하는 `while` 문을 작성하세요.
   - Step 3: `random.randint(1, 45)`를 사용하여 1~45 사이의 무작위 정수를 하나 생성합니다.
   - Step 4: 생성된 숫자가 리스트에 중복되지 않을 때만 `append()`를 사용하여 추가합니다.
   - Step 5: 숫자가 리스트에 추가될 때마다 몇 번째 숫자인지(1st, 2nd, 3rd...)와 값을 출력합니다.
   - Step 6: 반복이 끝나면 리스트를 오름차순으로 정렬(sort)하여 반환하세요.

3. 실행 예시

> {: .result .fs-3 } 
> > 🍀 금주의 로또 추천 번호 🍀  
> > [10, 11, 14, 31, 34, 37]

<\br>

```python
import random

def make_lotto():
    # 1. 숫자를 담을 빈 리스트 생성
    
    # 2. 리스트에 숫자가 6개가 될 때까지 무한 반복 (while 문 활용)
    # 중복된 숫자가 나올 수 있으므로 for 문보다는 while 문이 적합합니다.

    # 3. 1~45 사이의 랜덤 숫자 생성

        
    # 4. 중복 검사: 리스트에 없는 숫자인 경우에만 추가

    
    # 5. 생성된 6개의 숫자를 오름차순 정렬

    
    return lotto_list

# --- 함수 실행 및 출력 ---
my_numbers = make_lotto()
print("🍀 금주의 로또 추천 번호 🍀")
print(my_numbers)
```

---

### **4. 수치 적분을 이용한 면적 계산 (Integral by parts)**
구분구적법을 사용하여 곡선 아래의 면적을 구하는 '수치 적분' 프로그램을 작성해 봅시다.

![](https://bgjang-khu.github.io/MSE201/assets/images/integral.png)

**함수 작성 및 수행 조건**

1. 메쉬(Mesh) 생성: `np.linspace(0, 1, n)`을 사용하여 0부터 1 사이를 $n$개의 지점으로 나눈 선형 메쉬를 만드세요.

2. calc_area(n) 함수 작성:
    - 너비(width): 이웃한 두 $x$ 지점 사이의 간격을 계산합니다.
    - 높이(height): 각 구간의 $x$ 값에 대해 $x^2$을 계산하여 사각형의 높이로 삼습니다.
    - 합산: 모든 사각형의 면적(width * height)을 더해 전체 면적을 구합니다.

3. 오차 분석: 수치적으로 구한 면적과 이론적인 참값($\int_0^1 x^2 dx = 1/3$)을 비교하여 오차(error)를 계산합니다.

4. 정밀도 확인: $n$의 값을 10, 100, 1000으로 키워가며 오차가 줄어드는지 확인하세요.

<\br>

```python
import numpy as np

def calc_area(n):
    # 1. 0부터 1까지 n개의 지점으로 나누는 메쉬 생성 
    x = np.linspace(0, 1, n)
    
    # 2. 사각형 하나의 너비(dx) 계산 
    # 전체 구간(1-0)을 등분한 간격입니다.

    
    # 3. 사각형 면적의 총합 구하기 total_area
    # 오른쪽 끝점을 높이로 사용하는 방식의 합산

        
    # 4. 이론적인 참값(1/3)과 비교하여 오차 계산 
    exact_value = 1/3
    error = abs(exact_value - total_area)
    
    return total_area, error

# --- 프로그램 실행 및 정밀도 테스트 파트 ---
print("📊 Numerical Integration of y = x^2 from 0 to 1 📊")
print("-" * 50)

# 분할 수(n)를 다르게 하여 오차의 변화를 관찰합니다 
for n_size in [10, 100, 1000, 10000]:
    area, err = calc_area(n_size)
    print(f"분할 수(n): {n_size:5} | 추정 면적: {area:.6f} | 오차: {err:.6f}")

print("-" * 50)
```