---
layout: default
title: 반복문1 (For 문)
parent: Ch4. 제어문
nav_order: 2
math: mathjax
---

# 반복문1 (For Loops)
<br>
For 문은 리스트, 튜플, 문자열과 같은 **시퀀스(Sequence)** 내의 모든 요소에 대해 특정 코드 블록을 되풀이하여 실행하는 구문입니다. 참고 자료에서는 이를 **확정적 반복문(Definite loops)** 이라고도 부르는데, 시퀀스의 시작과 끝이 미리 정해져 있기 때문입니다.

## **🛠️ 기본 문법**

파이썬의 for 문은 문장 구조가 매우 직관적입니다.

```python
for 변수 in 시퀀스(리스트, 튜플, 문자열 등):
    수행할 문장 1
    수행할 문장 2
```

- **`for` 키워드**: 반복의 시작을 알립니다.
- **루프 변수 (Looping Variable)**: 시퀀스의 요소를 차례대로 하나씩 담는 바구니 역할을 합니다.
- **`in` 키워드**: 뒤에 올 시퀀스 데이터에서 값을 가져오겠다는 뜻입니다.
- **시퀀스**: 반복할 대상(리스트, 문자열 등)입니다. 첫 번째 요소부터 마지막까지 차례로 변수에 대입됩니다.
- **콜론(:)과 들여쓰기**: if 문과 마찬가지로 반복 실행될 문장들은 반드시 들여쓰기 되어야 합니다.

**🔄 동작 원리**

1. 시퀀스의 첫 번째 요소를 루프 변수에 대입합니다.
2. 들여쓰기 된 코드 블록을 실행합니다.
3. 시퀀스의 다음 요소가 있으면 다시 변수에 대입하고 코드 블록을 실행합니다.
4. 시퀀스의 마지막 요소까지 처리가 끝나면 반복문을 종료하고 다음 코드로 넘어갑니다.

---

### **🚀 TRY IT!**
{: .text-blue-200 }


```python
# 리스트 순회하기
fruits = ["apple", "banana", "cherry"]

for x in fruits:
    print(f"내가 좋아하는 과일은 {x}입니다.")

# 문자열 순회하기 (문자 하나씩 출력)
for char in "PYTHON":
    print(char)
```
> {: .result .fs-3 }  
> 내가 좋아하는 과일은 apple입니다.  
> 내가 좋아하는 과일은 banana입니다.  
> 내가 좋아하는 과일은 cherry입니다.  
> P  
> Y  
> T  
> H  
> O  
> N

## **🛠️ 연속된 숫자 생성기: `range()` 함수**

특정 횟수만큼 반복하거나 일정한 규칙의 숫자 리스트가 필요할 때 사용합니다. Python의 `range()`는 메모리를 효율적으로 쓰기 위해 숫자를 미리 다 만들어두지 않고, 필요할 때마다 하나씩 꺼내주는 함수이며, **`for` 문에서 시퀀스를 정해줄 때 가장 흔하게 사용됩니다**.


- `range(stop)` : 0부터 stop 미만까지 1씩 증가
- `range(start, stop)` : start부터 stop 미만까지 1씩 증가
- `range(start, stop, step)` : start부터 stop 미만까지 step만큼 증가

```python
for i in range(0,5):
    print(i)
```
> {: .result .fs-3 }
> 0  
> 1  
> 2  
> 3  
> 4

### **💡range vs. arange vs. linspace**

|**구분**|**`range()`**|**`np.arange()`**|**`np.arange()`**|
|:-------|:-------|:-------|:-------|
|특징1 | Python 기본 제공 | Numpy 라이브러리 |Numpy 라이브러리|
|특징2 | **정수(int)**만 가능 | 정수, **실수(float)** 모두 가능| 정수, **실수(float)** 모두 가능|
|특징3| 반복 횟수 제어용 | 시작/끝/**간격** 지정 |시작/끝/**개수** 지정|
|예시| `range(0, 5)` | `np.arange(0, 0.5, 0.1)` | `np.linspace(0, 1, 10)`|

---

### **🚀 TRY IT!**
{: .text-blue-200 }

간단한 반복문을 사용해 1에서 3까지의 모든 정수의 합을 구해봅시다.

```python
sum=0
for i in range(1,4):
    sum+=i
print(f'총합은 {sum}입니다')
```
> {: .result .fs-3 }
> 총합은 6입니다  

### **🤔 Wait and Think!**
위의 예시 코드를 실행할 때 어떤 일이 일어나고 있는 것일까요?

1. 우선 `range(1,4)` 함수는 1로 시작해 3에서 끝나는 리스트를 만듭니다.
2. 반복 1회차 
    - 변수 `i`에 첫번재 값이 1이 할당됩니다.
    - n=n+i 계산이 수행됩니다 (0+1=1)
    - 여기에서 n의 값은 1입니다.
3. 반복 2회차 
    - 변수 `i`에 두번재 값이 2이 할당됩니다.
    - n=n+i 계산이 수행됩니다 (1+2=3)
    - 여기에서 n의 값은 3입니다.
4. 반복 3회차 
    - 변수 `i`에 첫번재 값이 3이 할당됩니다.
    - n=n+i 계산이 수행됩니다 (3+3=6)
    - 여기에서 n의 값은 6입니다.
5. 리스트에 더 이상 할당할 값이 없으므로, `for`문이 종료됩니다. 최종적인 `n`의 값은 6이 됩니다.

---

### **🚀 TRY IT!**
{: .text-blue-200 }

반복문을 사용해서 리스트 안에 있는 숫자의 합을 구해봅시다.

```python
sum = 0
a = [2, 3, 1, 3, 3]
for i in a:
    sum += i 
print(sum)
```

`range()`함수와 `len()`함수를 사용하여 같은 동작을 하는 코드를 작성해봅시다.

```python
sum = 0
a = [2, 3, 1, 3, 3]
for i in range(0, len(a)):
    sum += a[i] 
print(sum)
```

---

### **🚀 TRY IT!**
{: .text-blue-200 }

리스트의 요솟값이 튜플인 경우는 다음과 같이 변수값에 받아올 수 있습니다.

```python
a=[(1,2), (3,4), (5,6)]
for (x,y) in a:
    print(x+y)
```
> {: .result .fs-3 }
> 3  
> 7  
> 11
<br>

```python
a=[('신소재',20250001), ('소재과',20250100), ('공과대',20250234)]
for (x,y) in a:
    print(f'{x}의 학번은 {y}입니다.')
```
> {: .result .fs-3 }  
> 신소재의 학번은 20250001입니다.  
> 소재과의 학번은 20250100입니다.  
> 공과대의 학번은 20250234입니다.

---

---

### **🚀 TRY IT!**
{: .text-blue-200 }

딕셔너리 자료형에서는 다음과 같이 `for` 문을 사용할 수 있습니다.

```python
dict_a = {"One":1, "Two":2, "Three":3}

for key in dict_a.keys():
    print(key, dict_a[key])
```
> {: .result .fs-3 }  
> One 1  
> Two 2  
> Three 3

위 예시에서는 우선 `key()` 메서드를 사용해 키 (key)들을 얻어오고, 키 (key)를 사용해서 해당하는 값 (value)에 접근했습니다.
하지만 아래와 같이 한번에 key와 value를 얻는 것도 가능합니다.

```python
dict_a = {"One":1, "Two":2, "Three":3}

for key, value in dict_a.items():
    print(key, value)
```

위 예시에서는 서로 다른 두 개의 루프 변수를 동시에 할당한 것입니다. 비슷한 방식으로 만약 두 개의 같은 길이를 갖는 리스트 (list)가 있다면 다음과 같이 `zip` 함수를 사용할 수 있습니다.

```python
a = ["One", "Two", "Three"]
b = [1, 2, 3]

for i, j in zip(a, b):
    print(i, j)
```

--- 

## **🛠️ 루프 제어문 (break, continue, pass)**

반복문이 실행되는 도중, 특정 조건에 따라 반복을 멈추거나 다음 순서로 넘어가야 할 때 사용하는 키워드들입니다.

### **🚫 break: "여기서 당장 멈춰!"**
`break` 키워드는 자신을 감싸고 있는 가장 가까운 반복문을 즉시 종료하고 빠져나옵니다.

🤔 언제 쓸까요? 무한 루프에서 특정 목표치에 도달했거나, 에러가 발생하여 더 이상 계산이 불가능할 때 사용합니다.

```python
for i in range(1, 11):
    if i == 5:
        break  # i가 5가 되는 순간 반복문을 완전히 끝냅니다.
    print(i)

# 출력 결과: 1, 2, 3, 4 (5부터는 출력되지 않음)
```

### **⏭️ continue: "이번 판은 건너뛰자!"**
`continue` 키워드는 현재 반복 주기의 남은 코드를 모두 무시하고, 시퀀스의 다음 요소로 즉시 넘어가 반복을 계속합니다.

🤔 언제 쓸까요? 특정 데이터(예: 음수 값이나 결측치)를 제외하고 계산을 이어가고 싶을 때 유용합니다.

```python
for i in range(1, 6):
    if i == 3:
        continue  # i가 3일 때는 아래 print를 무시하고 4로 넘어갑니다.
    print(i)

# 출력 결과: 1, 2, 4, 5 (3만 쏙 빠짐)
```

### **😶 pass: "일단 자리만 지키고 있어""**

`pass`는 아무런 동작도 하지 않는 키워드입니다. 문법적으로는 문장이 필요하지만, 실제로는 아무 일도 하고 싶지 않을 때 사용합니다.

🤔 언제 쓸까요? 나중에 코드를 작성할 부분을 미리 만들어 두거나(Placeholder), 조건문에서 아무 처리도 하지 않고 넘어가야 할 때 씁니다.

```python
for i in range(1, 4):
    if i == 2:
        pass  # 나중에 여기에 특별한 로직을 넣을 예정!
    print(i)

# 출력 결과: 1, 2, 3 (아무 변화 없음)
```

---

## **✍️ 연습 문제**

### **1. 별(*)로 피라미드 출력하기**
`for` 문과 문자열 포매팅을 사용하여 사용자로부터 입력받은 층수만큼 피라미드를 출력하는 코드를 완성하세요.

- 조건1: 피라미드의 층수를 입력받습니다.
- 조건2: 각 층의 별 개수는 1, 3, 5, 7, ... 순으로 늘어납니다.
- 조건3: 문자열의 총 길이 지정과 가운데 정렬(^) 기능을 사용하여 공백을 직접 계산하지 않고 출력해 봅시다.

```python
# 1. 피라미드 층수 입력 받기
layers = int(input("피라미드를 몇 층으로 쌓을까요? "))

# 2. 피라미드의 최대 너비 계산 (가장 아래층의 별 개수)
max_width = 2 * layers - 1

# 3. for 문을 이용한 출력
for i in                     
    # 별의 개수 계산 (1, 3, 5...)
    stars = "*" * 
    
    # {변수:^너비} 형식을 사용합니다.
    print(f"                    ")
```

### **2. 짝수 번째 데이터만 모으기**
기존 리스트에서 **짝수 번째(인덱스 0, 2, 4...)**에 위치한 데이터만 골라내어 새로운 리스트를 만드는 프로그램을 작성해 봅시다.

- 조건1: 빈 리스트 (`new_data`)를 미리 만들어둡니다.
- 조건2: `range()` 함수를 사용하여 짝수 인덱스를 생성합니다. (Hint: step을 활용합니다.)
- 조건3: `append()` 함수를 사용하여 해당 인덱서의 값을 새로운 리스트에 추가합니다.

```python
# 1. 원본 데이터 리스트
data = [1.2, 3.5, 2.0, 4.8, 7.1, 0.5, 9.3, 2.2]

# 2. 결과물을 담을 빈 리스트 생성
new_data = []

# 3. range(start, stop, step)을 활용한 인덱스 생성
for i in                    :
    # 생성된 인덱스 i를 사용하여 데이터 추출 후 새로운 리스트에 담기


    print(f"인덱스 {i}번의 데이터 {value}를 추출했습니다.")

# 4. 결과 출력
print("-" * 30)
print(f"최종 추출 리스트: {even_indexed_data}")
```

### **3. 원하는 범위의 구구단 출력하기 (Nested for loop)**
사용자로부터 시작 단과 종료 단을 입력받아, 그 사이의 모든 구구단을 출력하는 프로그램을 작성하세요.

- 조건1: 시작 단(`start_dan`)과 종료 단(`end_dan`)을 정수로 입력받습니다.
- 조건2: 바깥쪽 `for` 문은 입력받은 단의 범위를 반복합니다.
- 조건3: 안쪽 `for` 문은 각 단에 대해 1부터 9까지의 곱셈을 반복합니다.
- 조건4: 한 단이 끝나면 한 줄을 띄워 가독성을 높입니다.

다음 실행 예시를 참고하세요.

> {: .result .fs-3 }  
> > 시작할 단을 입력하세요: 3  
> > 종료할 단을 입력하세요: 4  
> > --- 3단 시작 ---  
> > 3 x 1 = 3  
> > 3 x 2 = 6  
> > ...중략...  
> > 3 x 8 = 24  
> > 3 x 9 = 27  
> >  
> > --- 4단 시작 ---  
> > 4 x 1 = 4  
> > 4 x 2 = 8  
> > 4 x 3 = 12  
> > ...생략...  


```python
# 1. 시작 단과 종료 단 입력 받기
start_dan = int(input("시작할 단을 입력하세요: "))
end_dan = int(input("종료할 단을 입력하세요: "))

# 2. 중첩 루프 설계
for i in                        
    print(f"--- {dan}단 시작 ---")

    for j in 
    
    print() # 한 단이 끝나면 한 줄을 띄워 가독성을 높입니다.
```


### **4. 실험 데이터 필터링 및 합계 구하기 (Loop + If)**
실험을 통해 얻은 데이터 리스트에서 양수(+)인 정상 데이터만 골라내어 합계를 구하고, 그 개수가 몇 개인지 판별하는 프로그램을 작성하세요. (음수 데이터는 노이즈라고 가정합니다.)

- 조건1: 리스트의 요소를 하나씩 꺼내어 검사합니다.
- 조건2: 음수 (`x < 0`)) 인 경우 `continue`를 사용하여 합계 계산을 건너뜁니다.
- 조건3: 양수인 경우에만 `total` 변수에 누적하고, `count` 변수를 1씩 증가시킵니다.

```python
data_list = [10.2, -5.5, 15.3, 20.0, -1.2, 7.8, 9.4, -0.8, 12.5, 15.3, 2.4, 5.6]

total = 0   # 합계를 저장할 변수
count = 0   # 데이터 개수를 저장할 변수

for                    
    # 1. 음수 데이터(노이즈)를 만나면 다음 데이터로 점프
    if 

    # 2. 양수 데이터인 경우에만 total 과 count를 업데이트
    total 
    count 

print(f"분석 결과: 총 {count}개의 유효 데이터를 발견했습니다.")
print(f"유효 데이터의 총합: {total:.2f}")
```

### **5. 합격 판별 (Loop + If)**
학생 점수가 담긴 리스트를 순차적으로 검사하여, 각 학생의 번호와 합격/불합격 여부를 출력하는 프로그램을 작성하세요.

- 조건1: 60점 이상이면 합격, 60점 미만이면 불합격입니다.
- 조건2 (출력 형식)`n번 학생은 00 점으로 합격 (혹은 불합격) 입니다`

> {: .result .fs-3 }  
> 1번 학생은 90점으로 합격입니다.
> 2번 학생은 25점으로 불합격입니다.  

```python
# 1. 학생들의 시험 점수 리스트 정의
marks = [90, 25, 67, 45, 80, 100, 10, 33, 56, 62, 73, 45]
# 2. 학생 번호를 부여하기 위한 변수 초기화
num = 0
# 3. for 문을 이용한 점수 검사
for 
    num
    
    if 
        print(f"{num}번 학생은 합격입니다.")
    else:
        print(f"{num}번 학생은 불합격입니다.")
```